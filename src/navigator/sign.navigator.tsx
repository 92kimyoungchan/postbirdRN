import React from 'react';
import {createStackNavigator} from '@react-navigation/stack';
i;



const SignNavigator = () => {
    const Stack = createStackNavigator();
    return (
        <Stack.Navigator>
            
        </Stack.Navigator>
    )
};

export default observer(SignNavigator);
